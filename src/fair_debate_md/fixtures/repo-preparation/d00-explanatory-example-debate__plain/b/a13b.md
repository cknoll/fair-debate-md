This is an answer to statement `a13` (first entry in the list). It itself is also a contribution. Because it refers to statement `a13` its contribution key is `a13b`. This contribution consequently also consists of referenceable and answerable statements (keys: `a13b1`, `a13b2` etc.) By the way: you can see the key of each statement by hovering over it with the mouse. Clicking or tapping on a statement displays its full URL (including a copy-button).

Note that this answer contribution is distinguished by color and indentation from the text of contribution `a`.
